# calculate the area of the rectangle

length = input("Enter the length : ")
width = input("Enter the width : ")
length = float(length)
width = float(width)
area = length * width
print(f"thea area of the rectangle is : {area} cm")
